#include "menu.h"

Menu MENU {
    POPUP "&FILE" {
        MENUITEM "&Open file...", IDM_OPEN
        MENUITEM "&Close file", IDM_CLOSE
        MENUITEM "&Exit", IDM_EXIT
    }

    POPUP "&VIEW" {
        MENUITEM "&Layout OFF", IDM_WRAP_OFF
        MENUITEM "&Layout ON", IDM_WRAP_ON
    }
}
